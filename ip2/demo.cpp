#include "tree234.h"
#include <iostream>

using namespace Tree234Namespace;

int main(int argc, char *argv[]) {
    Tree234<int> tree;

    // std::cout << "Inset 1 to tree\n";
    // tree.insert_val(1);
    //
    // std::cout << "Conatains 1:" << (tree.contains_val(1) ? "yes" : "no") << "\n";
    // std::cout << "Conatains 2:" << (tree.contains_val(2) ? "yes" : "no") << "\n";

    return 0;
}
